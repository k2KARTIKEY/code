#include<stdio.h>
#include<string.h>
int main(){
    char str[40];
    scanf("%s",str);// only first word will be consider...
   // gets(str); // entire sentence will be print ....
printf("your name: %s",str);
}