#include<iostream>
using namespace std;

int main(){
    int n;
    cin>>n;
// syntax for ternary oprator
//(condition) ? if true : if flase ;
(n%2==0) ? cout<<"no. is even" : cout<<"no. is odd";
return 0;
}
